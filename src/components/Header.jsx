import React from 'react';
import '../styles/components/Header.css'; // Import the CSS file for Header

const Header = () => {
  return (
    <header className="header">
      Welcome to My Website
    </header>
  );
};

export default Header;
